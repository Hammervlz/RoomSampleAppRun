package com.maximov.roomsampleapp

import android.app.Application

class MyApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        // Здесь можно инициализировать глобальные компоненты, если необходимо
    }
}
